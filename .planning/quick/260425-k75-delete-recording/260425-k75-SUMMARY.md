---
status: complete
plan: 260425-k75
description: Add the ability to delete a recording
started: 2026-04-25
completed: 2026-04-25
---

## Summary

Added full vertical slice for jam deletion: database cascade delete, file cleanup, Tauri command, and UI button with confirmation dialog.

## Key Files

### Created
- None (all modifications to existing files)

### Modified
- `crates/wallflower-core/src/db/mod.rs` — `delete_jam()` function: clears FTS5 index, deletes jam row (cascades to 12+ child tables)
- `crates/wallflower-app/src/commands/jams.rs` — `delete_jam` Tauri command: DB delete + best-effort file cleanup (audio, peaks, photos, stem cache)
- `crates/wallflower-app/src/lib.rs` — registered `delete_jam` in `generate_handler!`
- `src/lib/tauri.ts` — `deleteJam()` IPC wrapper
- `src/components/library/JamDetail.tsx` — Trash icon button in header, confirmation dialog with "Keep Recording" / "Delete" actions

## Self-Check: PASSED

- `cargo test --workspace` passes
- `npx tsc --noEmit` passes
- Delete button visible in JamDetail header
- Confirmation dialog follows existing BookmarkContextMenu pattern
- Cache invalidation covers both "jams" and "samples" query keys

## Checkpoint: Human Verification Required

1. Open app, select a jam from library
2. Verify trash icon in top-right of detail header
3. Click trash → confirmation dialog appears
4. Click "Keep Recording" → dialog closes, nothing happens
5. Click trash again → "Delete" → toast "Jam deleted", returned to library, jam gone
