---
phase: 03-recording-engine-system-integration
plan: 05
type: execute
wave: 4
depends_on: ["03-04"]
files_modified:
  - src/components/settings/SettingsPage.tsx
autonomous: false
requirements: [REC-04, REC-09, INFRA-10, INFRA-12]

must_haves:
  truths:
    - "User can configure silence threshold (-60dB to -20dB) in Settings"
    - "User can view/change the global recording hotkey in Settings"
    - "Full recording workflow works end-to-end: start via hotkey or button, record with live feedback, stop with confirmation, recording appears in library"
    - "System tray shows recording state and provides quick actions"
    - "Crash recovery imports incomplete recordings on startup"
  artifacts:
    - path: "src/components/settings/SettingsPage.tsx"
      provides: "Settings page with recording section: silence threshold slider and hotkey display"
  key_links:
    - from: "src/components/settings/SettingsPage.tsx"
      to: "src/lib/tauri.ts"
      via: "Settings save via Tauri commands"
      pattern: "updateSettings"
---

<objective>
Add recording settings (silence threshold, hotkey config) to the Settings page, and perform a full end-to-end verification of the recording workflow including system tray, global hotkey, crash recovery, and live metadata editing.

Purpose: Complete the recording feature with user-configurable settings and verify the full workflow matches all 13 user decisions (D-01 through D-13).

Output: Updated Settings page with recording settings. Full phase verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-recording-engine-system-integration/03-CONTEXT.md
@.planning/phases/03-recording-engine-system-integration/03-UI-SPEC.md
@.planning/phases/03-recording-engine-system-integration/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add recording settings to Settings page</name>
  <files>
    src/components/settings/SettingsPage.tsx
  </files>
  <read_first>
    src/components/settings/SettingsPage.tsx,
    src/lib/tauri.ts,
    .planning/phases/03-recording-engine-system-integration/03-UI-SPEC.md
  </read_first>
  <action>
    Add a "Recording" settings group to the existing Settings page, following the card-based layout pattern already established.

    1. Add a new card section titled "Recording" after existing settings cards.

    2. Silence Threshold setting (D-03, REC-04):
       - Label: "Silence Threshold" (per UI-SPEC copywriting)
       - Description: "Mark sections below this volume as silent. Quieter values catch more silence." (per UI-SPEC)
       - Control: HTML range input (slider), min=-60, max=-20, step=1, default=-40
       - Display current value: "{value}dB" to the right of the slider
       - Slider styling: 200px width, 4px track height via CSS. Track color: #323844. Thumb: accent #E8863A. Fill: accent.
       - On change: save to settings via existing `updateSettings` Tauri command. The settings model in wallflower-core needs a `silence_threshold_db` field. If not present, add it to the settings schema (or use a sensible default).
       - Use CSS for range input styling:
         ```css
         input[type="range"] { -webkit-appearance: none; background: #323844; height: 4px; border-radius: 2px; }
         input[type="range"]::-webkit-slider-thumb { -webkit-appearance: none; width: 16px; height: 16px; border-radius: 50%; background: #E8863A; border: 2px solid white; cursor: pointer; }
         ```

    3. Record Shortcut setting (D-08, D-09):
       - Label: "Record Shortcut" (per UI-SPEC)
       - Description: "Start and stop recording from any app. Default: Cmd+Shift+R" (per UI-SPEC)
       - Display: Show current shortcut in a styled badge/chip (e.g., "Cmd+Shift+R" in a 14px body text, secondary background, border, rounded)
       - For v1: Display-only (not editable). The shortcut is hardcoded as Cmd+Shift+R. Add a note: "Shortcut customization coming in a future update." This avoids the complexity of a full hotkey capture widget.
       - Alternatively, if time permits, implement a basic hotkey capture: click the input, press desired key combo, display result. This requires re-registering the global shortcut via Tauri command.

    4. Ensure the settings values persist: the silence threshold should be loaded from the settings store on mount and saved on change. The hotkey display reads from a constant or settings.
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Silence Threshold" src/components/settings/SettingsPage.tsx` exists
    - `grep "Record Shortcut" src/components/settings/SettingsPage.tsx` exists
    - `grep "Cmd+Shift+R" src/components/settings/SettingsPage.tsx` exists
    - `grep "Mark sections below this volume as silent" src/components/settings/SettingsPage.tsx` exists (per UI-SPEC)
    - `grep 'type="range"' src/components/settings/SettingsPage.tsx` exists (slider input)
    - `npx tsc --noEmit` passes
  </acceptance_criteria>
  <done>Settings page has Recording section with silence threshold slider and record shortcut display. Values persist via settings store.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full Phase 3 recording workflow verification</name>
  <files>N/A</files>
  <action>
    User verifies the complete Phase 3 recording workflow end-to-end. All code is already built by Plans 01-04 and Task 1 above. This checkpoint confirms the integrated system works as specified.

    What was built:
    - Audio recording from default input device via cpal
    - Crash-safe WAV writing with periodic flush/fsync (recoverable on crash)
    - Transport bar transformation showing recording state (red accent, elapsed time, level meter, device info)
    - Live waveform growing during recording
    - Stop-recording confirmation dialog (Keep Recording auto-focused)
    - Navigation lock during recording (tabs and back disabled)
    - Metadata editing during recording (tags, collaborators, instruments, notes, photos)
    - System tray icon with recording status and Start/Stop/Open/Quit actions
    - Global hotkey Cmd+Shift+R for start/stop recording toggle
    - Device disconnect detection and reconnection handling
    - Silence detection with visual markers on waveform
    - Priority scheduler pausing background tasks during recording
    - Crash recovery importing incomplete WAV files on startup
    - Recording settings in Settings page (silence threshold, hotkey display)

    Verification steps:
    1. Run `cargo tauri dev` to launch the app
    2. Verify the app starts without errors
    3. Connect an audio interface (or use built-in mic)
    4. Click the record button -- verify transport bar transforms to recording mode
    5. Verify live waveform and metadata editing work during recording
    6. Verify navigation is locked during recording
    7. Stop recording via dialog -- verify recording appears in library
    8. Test system tray icon and menu actions
    9. Test global hotkey Cmd+Shift+R from another app
    10. Test crash recovery (force-quit during recording, relaunch)
    11. Check recording settings in Settings page
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && cargo test --workspace 2>&1 | tail -5 && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <done>User has verified the complete recording workflow end-to-end. All 13 user decisions (D-01 through D-13) and all 11 requirement IDs are confirmed working.</done>
</task>

</tasks>

<verification>
- `cargo tauri build` succeeds
- `cargo test --workspace` passes
- `npx tsc --noEmit` passes
- Full recording workflow verified by user
</verification>

<success_criteria>
Phase 3 is complete: users can record audio with crash safety, live metadata editing, system tray integration, global hotkey control, and device disconnect recovery. All 13 user decisions (D-01 through D-13) are implemented. All 11 requirement IDs are satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/03-recording-engine-system-integration/03-05-SUMMARY.md`
</output>
