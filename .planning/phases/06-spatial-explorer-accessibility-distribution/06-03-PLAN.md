---
phase: 06-spatial-explorer-accessibility-distribution
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/app/page.tsx
  - src/components/library/Timeline.tsx
  - src/components/library/JamDetail.tsx
  - src/components/library/JamCard.tsx
  - src/components/transport/TransportBar.tsx
  - src/components/waveform/WaveformDetail.tsx
  - src/components/waveform/WaveformOverview.tsx
  - src/components/metadata/TagChips.tsx
  - src/components/metadata/MetadataEditor.tsx
  - src/components/analysis/FilterBar.tsx
  - src/components/recording/RecordingView.tsx
  - src/components/settings/SettingsPage.tsx
autonomous: true
requirements:
  - DES-02
  - DES-03
  - DES-04

must_haves:
  truths:
    - "User can navigate all interactive elements using Tab and arrow keys"
    - "Screen reader announces jam cards, transport controls, waveform position, and recording status"
    - "ARIA landmarks identify navigation, main content, and playback controls"
    - "Tab key moves through jam cards in the timeline"
    - "Arrow keys navigate between tag chips"
    - "Escape from jam detail returns to library"
  artifacts:
    - path: "src/components/library/Timeline.tsx"
      provides: "Roving tabindex on jam card list"
      contains: "useRovingTabIndex"
    - path: "src/components/transport/TransportBar.tsx"
      provides: "Toolbar role with labeled buttons"
      contains: "role=\"toolbar\""
    - path: "src/components/waveform/WaveformDetail.tsx"
      provides: "ARIA live region for playback position"
      contains: "aria-live"
  key_links:
    - from: "src/components/library/Timeline.tsx"
      to: "src/components/accessibility/useRovingTabIndex.ts"
      via: "import useRovingTabIndex"
      pattern: "useRovingTabIndex"
    - from: "src/app/page.tsx"
      to: "ARIA landmarks"
      via: "role attributes on tab bar, main, transport"
      pattern: "role=\"banner\""
---

<objective>
Retrofit keyboard navigation, ARIA labels, and screen reader support across all existing application components.

Purpose: Make the entire application accessible to keyboard-only users and screen reader users. This is a retrofit — existing components keep their visual appearance, with added ARIA attributes, keyboard handlers, and landmark roles. Priority order per D-06: Library browse > Jam detail > Playback controls > Recording flow > Settings.

Output: All interactive elements keyboard-navigable, screen readers announce all state changes, ARIA landmarks identify app regions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-spatial-explorer-accessibility-distribution/06-CONTEXT.md
@.planning/phases/06-spatial-explorer-accessibility-distribution/06-UI-SPEC.md
@.planning/phases/06-spatial-explorer-accessibility-distribution/06-01-SUMMARY.md

@src/app/page.tsx
@src/components/accessibility/index.ts
@src/components/accessibility/useRovingTabIndex.ts

<interfaces>
From src/components/accessibility/index.ts (created by Plan 01):
```typescript
export { SkipLink } from './SkipLink';
export { HighContrastProvider, useHighContrast } from './HighContrastProvider';
export { useRovingTabIndex } from './useRovingTabIndex';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ARIA landmarks and keyboard navigation for Library, JamDetail, TransportBar</name>
  <files>
    src/app/page.tsx,
    src/components/library/Timeline.tsx,
    src/components/library/JamCard.tsx,
    src/components/library/JamDetail.tsx,
    src/components/transport/TransportBar.tsx
  </files>
  <read_first>
    src/app/page.tsx,
    src/components/library/Timeline.tsx,
    src/components/library/JamCard.tsx,
    src/components/library/JamDetail.tsx,
    src/components/transport/TransportBar.tsx,
    src/components/accessibility/useRovingTabIndex.ts
  </read_first>
  <action>
    **1. ARIA landmarks in page.tsx:**
    Add landmark roles to the app shell. NOTE: page.tsx may already be modified by Plan 02 (Explore tab). Read current state before editing.
    - Tab bar container: `role="banner"` and `aria-label="Wallflower navigation"`
    - Main content area: already has `id="main-content"` from Plan 01. Add `role="main"` and `aria-label="Main content"`
    - Transport bar (if rendered in page.tsx): `role="contentinfo"` and `aria-label="Playback controls"`

    **2. Timeline.tsx — roving tabindex on jam card list:**
    Import `useRovingTabIndex` from `@/components/accessibility`. The Timeline renders a list of JamCards. Add:
    - Container: `role="listbox"` and `aria-label="Jam library"`
    - Extract jam IDs from the rendered list to pass to `useRovingTabIndex(jamIds, activeJamId, { orientation: 'vertical' })`
    - Pass `ref`, `tabIndex`, and `onKeyDown` from the hook to each JamCard
    - When a JamCard receives focus, set it as the active roving item
    - Enter key on a focused JamCard navigates to jam detail

    **3. JamCard.tsx — accessible card:**
    - Add `role="option"` to each card element
    - Add `aria-selected={isSelected}` (true when this is the focused/selected jam)
    - Add `aria-label` with concise announcement per D-08: `"{jam name}, {duration formatted}"` (e.g., "Friday Session, 3 minutes 42 seconds")
    - Accept `ref`, `tabIndex`, `onKeyDown`, `onFocus` props from parent (forwarded from useRovingTabIndex)
    - Use `React.forwardRef` if not already

    **4. JamDetail.tsx — heading hierarchy and escape navigation:**
    - Jam name: `<h1>` (if not already)
    - Section headings (metadata, analysis, bookmarks): `<h2>`
    - Add `onKeyDown` handler at the detail container level: Escape key calls `onBack()` to return to library
    - Add `aria-label="Jam detail: {jam name}"` on the container

    **5. TransportBar.tsx — toolbar role and button labels:**
    - Container: `role="toolbar"` and `aria-label="Playback controls"`
    - Verify each button has `aria-label`: "Previous track", "Play" / "Pause" (toggle based on state), "Next track"
    - Playback position display: add `aria-live="polite"` on the time display element so screen readers announce seek position changes
    - Ensure the existing global Space key handler is NOT overridden by any of these changes (Space must still toggle play/pause globally)
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && grep -l 'role="listbox"' src/components/library/Timeline.tsx && grep -l 'role="toolbar"' src/components/transport/TransportBar.tsx && grep -l 'role="option"' src/components/library/JamCard.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/app/page.tsx contains `role="banner"` and `aria-label="Wallflower navigation"`
    - src/components/library/Timeline.tsx contains `useRovingTabIndex` import and `role="listbox"` and `aria-label="Jam library"`
    - src/components/library/JamCard.tsx contains `role="option"` and `aria-selected` and `aria-label` and `forwardRef`
    - src/components/library/JamDetail.tsx contains `<h1` (for jam name) and `Escape` key handler and `onBack`
    - src/components/transport/TransportBar.tsx contains `role="toolbar"` and `aria-label="Playback controls"` and `aria-live="polite"`
  </acceptance_criteria>
  <done>Library browse, jam detail, and transport bar are fully keyboard-navigable with proper ARIA landmarks and screen reader announcements. Arrow keys navigate jam cards, Escape returns from detail view, transport bar is a labeled toolbar.</done>
</task>

<task type="auto">
  <name>Task 2: Accessibility retrofit for waveform, metadata, filter bar, recording, settings</name>
  <files>
    src/components/waveform/WaveformDetail.tsx,
    src/components/waveform/WaveformOverview.tsx,
    src/components/metadata/TagChips.tsx,
    src/components/metadata/MetadataEditor.tsx,
    src/components/analysis/FilterBar.tsx,
    src/components/recording/RecordingView.tsx,
    src/components/settings/SettingsPage.tsx
  </files>
  <read_first>
    src/components/waveform/WaveformDetail.tsx,
    src/components/waveform/WaveformOverview.tsx,
    src/components/metadata/TagChips.tsx,
    src/components/metadata/MetadataEditor.tsx,
    src/components/analysis/FilterBar.tsx,
    src/components/recording/RecordingView.tsx,
    src/components/settings/SettingsPage.tsx,
    src/components/accessibility/useRovingTabIndex.ts
  </read_first>
  <action>
    NOTE: Read each file first. Some may have different names or structures than listed. Adapt the retrofit to the actual component structure found.

    **1. WaveformDetail.tsx — keyboard seek and ARIA:**
    - Add `role="slider"` to the waveform container div (the one wrapping the wavesurfer instance)
    - Add `aria-label="Waveform — use left and right arrow keys to seek"`, `aria-valuemin="0"`, `aria-valuemax="{duration}"`, `aria-valuenow="{currentTime}"`, `aria-valuetext="{formatted time}"`
    - Add `tabIndex={0}` to make it focusable
    - Add `onKeyDown` handler:
      - ArrowLeft: seek back 5 seconds. Call wavesurfer.seekTo(clampedPosition).
      - ArrowRight: seek forward 5 seconds
      - Shift+ArrowLeft: seek back 30 seconds
      - Shift+ArrowRight: seek forward 30 seconds
      - Call `e.preventDefault()` for these keys to prevent page scroll
    - Add ARIA live region: `<div aria-live="polite" className="sr-only">{formatted current time}</div>` that updates when seek occurs (debounce to avoid excessive announcements — update only on keydown seek, not continuous playback)

    **2. WaveformOverview.tsx — slider role:**
    - Add `role="slider"` and `aria-label="Waveform overview — click to seek"` to the overview container
    - Add `aria-valuemin="0"`, `aria-valuemax="{duration}"`, `aria-valuenow="{currentTime}"`
    - Add `tabIndex={0}` for focusability

    **3. TagChips.tsx (or equivalent tag chip component) — roving tabindex:**
    If there is a component rendering tag chips (may be inline in MetadataEditor):
    - Import `useRovingTabIndex` from `@/components/accessibility`
    - Container: `role="listbox"` with `aria-label="Tags"`
    - Each chip: `role="option"`, `aria-label="{tag name}, tag, {position} of {total}"`
    - Arrow Left/Right moves focus between chips
    - Delete/Backspace key on focused chip calls the remove handler
    - Tab moves focus out of the chip group to the next control (e.g., add button)

    **4. MetadataEditor.tsx — fieldset grouping:**
    - Wrap each metadata section (Tags, Collaborators, Instruments, Location, Notes, Patch Notes) in `<fieldset>` with `<legend>` labeling the section
    - Ensure all input fields have associated `<label>` elements or `aria-label` attributes
    - The "Add" buttons should have descriptive labels: `aria-label="Add tag"`, `aria-label="Add collaborator"`, etc.

    **5. FilterBar.tsx — search landmark:**
    - Container: `role="search"` and `aria-label="Filter jams"`
    - Add ARIA live region: `<div aria-live="polite" className="sr-only">{count} jams matching</div>` that announces filter result count when filters change
    - Ensure all filter controls (Select dropdowns, text inputs) have `aria-label` attributes

    **6. RecordingView.tsx — live region for recording state:**
    - Add ARIA live region: `<div aria-live="assertive" className="sr-only">` that announces "Recording started" when recording begins and "Recording stopped, {duration}" when recording ends
    - Add `aria-live="polite"` on the elapsed time display for periodic time announcements
    - Ensure the recording indicator dot has `aria-hidden="true"` (decorative)

    **7. SettingsPage.tsx — form navigation:**
    - Add heading hierarchy: `<h1>Settings</h1>` for the page title
    - Each settings section should have `<h2>` headings
    - Add a note about high contrast: below the display section (or create one), add a read-only info line: "High contrast mode follows your macOS system setting (Accessibility > Display > Increase contrast)."
    - Ensure all toggle/checkbox/select controls have associated labels
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/components/waveform/WaveformDetail.tsx contains `role="slider"` and `aria-label` and `ArrowLeft` and `ArrowRight` and `aria-live`
    - src/components/waveform/WaveformOverview.tsx contains `role="slider"` and `aria-label="Waveform overview`
    - src/components/metadata/MetadataEditor.tsx contains `<fieldset` or `fieldset` and `<legend` or `legend`
    - src/components/analysis/FilterBar.tsx contains `role="search"` and `aria-label="Filter jams"` and `aria-live`
    - src/components/recording/RecordingView.tsx contains `aria-live` and `Recording started` or `recording started`
    - src/components/settings/SettingsPage.tsx contains `<h1` and `High contrast mode follows`
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All existing application components have ARIA labels, keyboard navigation, screen reader announcements, and landmark roles. Waveform supports keyboard seek (arrow keys). Tag chips support roving tabindex. Filter bar has search landmark with result count announcements. Recording view announces state changes. Settings has proper heading hierarchy.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- `grep -rn 'role="toolbar"\|role="listbox"\|role="search"\|role="slider"\|role="banner"\|role="option"' src/components/ src/app/` returns matches for all landmark/role types
- `grep -rn "aria-live" src/components/` returns matches in waveform, transport, recording, filter, and explore components
- `grep -rn "useRovingTabIndex" src/components/` returns matches in Timeline.tsx and TagChips.tsx (or MetadataEditor)
</verification>

<success_criteria>
Every interactive element in the application is keyboard-navigable. Screen readers announce all state changes and interactive elements. ARIA landmarks identify app regions. The retrofit covers Library browse, Jam detail, Playback controls, Waveform, Metadata editor, Filter bar, Recording view, and Settings — matching the priority order from D-06.
</success_criteria>

<output>
After completion, create `.planning/phases/06-spatial-explorer-accessibility-distribution/06-03-SUMMARY.md`
</output>
