---
phase: 06-spatial-explorer-accessibility-distribution
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/wallflower-app/Cargo.toml
  - crates/wallflower-app/src/lib.rs
  - crates/wallflower-app/capabilities/default.json
  - crates/wallflower-app/tauri.conf.json
  - src/components/settings/SettingsPage.tsx
  - src/components/settings/AutoLaunchSection.tsx
  - src/components/settings/FirstLaunchDialog.tsx
  - src/lib/tauri.ts
  - .github/workflows/release.yml
  - package.json
autonomous: false
requirements:
  - INFRA-13
  - INFRA-14

user_setup:
  - service: Apple Developer
    why: "Code signing and notarization for macOS distribution"
    env_vars:
      - name: APPLE_CERTIFICATE
        source: "Keychain Access -> export Developer ID Application certificate as .p12 -> base64 encode"
      - name: APPLE_CERTIFICATE_PASSWORD
        source: "Password set during .p12 export"
      - name: APPLE_SIGNING_IDENTITY
        source: "Developer ID Application: Your Name (TEAM_ID) — from Keychain Access certificate details"
      - name: APPLE_ID
        source: "Your Apple ID email used for Apple Developer account"
      - name: APPLE_PASSWORD
        source: "App-specific password from appleid.apple.com -> Sign-In and Security -> App-Specific Passwords"
      - name: APPLE_TEAM_ID
        source: "Apple Developer account -> Membership -> Team ID"
      - name: KEYCHAIN_PASSWORD
        source: "Any random password for the temporary CI keychain (e.g., generate with openssl rand -hex 16)"
    dashboard_config:
      - task: "Export Developer ID Application certificate as .p12"
        location: "Keychain Access -> My Certificates -> Developer ID Application -> Export"
      - task: "Create app-specific password"
        location: "appleid.apple.com -> Sign-In and Security -> App-Specific Passwords"
      - task: "Add all secrets to GitHub repository"
        location: "GitHub repo -> Settings -> Secrets and variables -> Actions -> New repository secret"

must_haves:
  truths:
    - "User can enable/disable auto-launch on login from Settings"
    - "First launch shows a dialog asking about auto-launch preference"
    - "GitHub Actions workflow exists for building, signing, notarizing, and releasing"
    - "Tauri config includes signing identity environment variables"
  artifacts:
    - path: "src/components/settings/AutoLaunchSection.tsx"
      provides: "Auto-launch toggle in settings"
      contains: "enable"
    - path: "src/components/settings/FirstLaunchDialog.tsx"
      provides: "First-launch auto-launch dialog"
      contains: "Start Wallflower on login"
    - path: ".github/workflows/release.yml"
      provides: "CI/CD release pipeline"
      contains: "tauri-apps/tauri-action"
    - path: "crates/wallflower-app/Cargo.toml"
      provides: "Autostart plugin dependency"
      contains: "tauri-plugin-autostart"
  key_links:
    - from: "src/components/settings/AutoLaunchSection.tsx"
      to: "@tauri-apps/plugin-autostart"
      via: "enable/disable/isEnabled JS API"
      pattern: "plugin-autostart"
    - from: ".github/workflows/release.yml"
      to: "tauri-apps/tauri-action"
      via: "GitHub Action for build+sign+notarize"
      pattern: "tauri-action"
---

<objective>
Auto-launch on login and macOS distribution pipeline — the final infrastructure for shipping Wallflower.

Purpose: Enable the app to start automatically on login (per D-13) and create a repeatable CI/CD pipeline for building, signing, notarizing, and distributing the app as a .dmg (per D-14). This is the last piece needed to ship Wallflower to other macOS users.

Output: Auto-launch toggle in Settings, first-launch dialog, Tauri autostart plugin integration, GitHub Actions release workflow, Tauri signing configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-spatial-explorer-accessibility-distribution/06-CONTEXT.md
@.planning/phases/06-spatial-explorer-accessibility-distribution/06-RESEARCH.md

@crates/wallflower-app/Cargo.toml
@crates/wallflower-app/src/lib.rs
@crates/wallflower-app/capabilities/default.json
@crates/wallflower-app/tauri.conf.json
@src/components/settings/SettingsPage.tsx
@src/lib/tauri.ts
@package.json

<interfaces>
From src/components/settings/SettingsPage.tsx:
```typescript
interface SettingsPageProps {
  onBack: () => void;
}
export function SettingsPage({ onBack }: SettingsPageProps) { ... }
// Uses getSettings/updateSettings from lib/tauri
// Uses AppSettings type from lib/types
```

From src/lib/types.ts:
```typescript
export interface AppSettings {
  watchFolder: string;
  silenceThresholdDb: number;
  exportRoot: string;
  exportFormat: string;
  exportBitDepth: number;
  analysisProfile: string;
}
```

From crates/wallflower-app/capabilities/default.json:
```json
{
  "permissions": [
    "core:default", "core:window:default", "core:window:allow-close",
    "core:window:allow-set-title", "notification:default", ...
  ]
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tauri autostart plugin integration and auto-launch UI</name>
  <files>
    crates/wallflower-app/Cargo.toml,
    crates/wallflower-app/src/lib.rs,
    crates/wallflower-app/capabilities/default.json,
    src/components/settings/AutoLaunchSection.tsx,
    src/components/settings/FirstLaunchDialog.tsx,
    src/components/settings/SettingsPage.tsx,
    src/lib/tauri.ts,
    package.json
  </files>
  <read_first>
    crates/wallflower-app/Cargo.toml,
    crates/wallflower-app/src/lib.rs,
    crates/wallflower-app/capabilities/default.json,
    src/components/settings/SettingsPage.tsx,
    src/lib/tauri.ts,
    src/lib/types.ts,
    package.json
  </read_first>
  <action>
    **1. Install npm dependency:**
    ```bash
    npm install @tauri-apps/plugin-autostart
    ```

    **2. Add Rust dependency to `crates/wallflower-app/Cargo.toml`:**
    Add to `[dependencies]`:
    ```toml
    tauri-plugin-autostart = { version = "2", features = ["default"] }
    ```

    **3. Register autostart plugin in `crates/wallflower-app/src/lib.rs`:**
    Find the Tauri builder chain (`.plugin(...)` calls). Add:
    ```rust
    use tauri_plugin_autostart::MacosLauncher;
    // In the builder chain:
    .plugin(tauri_plugin_autostart::init(MacosLauncher::LaunchAgent, Some(vec!["--autostarted"])))
    ```

    **4. Add autostart permissions to `capabilities/default.json`:**
    Add to the `permissions` array:
    ```json
    "autostart:allow-enable",
    "autostart:allow-disable",
    "autostart:allow-is-enabled"
    ```

    **5. Create `src/components/settings/FirstLaunchDialog.tsx`:**
    Dialog shown on first app launch to ask about auto-launch preference. Uses shadcn Dialog:
    ```tsx
    import { Dialog, DialogContent, DialogHeader, DialogTitle, DialogDescription, DialogFooter } from "@/components/ui/dialog";
    import { Button } from "@/components/ui/button";
    import { enable } from '@tauri-apps/plugin-autostart';
    import { setAutoLaunchDialogShown } from "@/lib/tauri"; // new helper

    interface FirstLaunchDialogProps {
      open: boolean;
      onClose: () => void;
    }

    export function FirstLaunchDialog({ open, onClose }: FirstLaunchDialogProps) {
      const handleAccept = async () => {
        try {
          await enable();
        } catch (e) { /* non-critical */ }
        await setAutoLaunchDialogShown();
        onClose();
      };
      const handleDecline = async () => {
        await setAutoLaunchDialogShown();
        onClose();
      };
      return (
        <Dialog open={open} onOpenChange={(o) => !o && handleDecline()}>
          <DialogContent>
            <DialogHeader>
              <DialogTitle>Start Wallflower on login?</DialogTitle>
              <DialogDescription>
                Wallflower can start automatically when you log in so it's ready to record whenever inspiration strikes.
              </DialogDescription>
            </DialogHeader>
            <p className="text-xs text-muted-foreground">You can change this later in Settings.</p>
            <DialogFooter>
              <Button variant="ghost" onClick={handleDecline}>Not now</Button>
              <Button onClick={handleAccept}>Yes, auto-launch</Button>
            </DialogFooter>
          </DialogContent>
        </Dialog>
      );
    }
    ```

    **6. Create `src/components/settings/AutoLaunchSection.tsx`:**
    Settings section with toggle for auto-launch. Uses `@tauri-apps/plugin-autostart` JS API:
    ```tsx
    import { enable, disable, isEnabled } from '@tauri-apps/plugin-autostart';
    ```
    - On mount: call `isEnabled()` to get current state
    - Toggle: calls `enable()` or `disable()` based on new state
    - Label: "Launch on login" (heading/label)
    - Description: "Start Wallflower automatically when you log in to macOS." (body/muted)
    - Use a shadcn Switch or checkbox control

    **7. Add auto-launch dialog shown helper to `src/lib/tauri.ts`:**
    Add a `setAutoLaunchDialogShown()` function that calls `invoke("update_settings", { key: "auto_launch_dialog_shown", value: "true" })` — uses the existing settings key-value store in SQLite.
    Add a `getAutoLaunchDialogShown()` function that calls `invoke("get_setting", { key: "auto_launch_dialog_shown" })` and returns boolean.

    **8. Wire into SettingsPage.tsx:**
    Import `AutoLaunchSection` and add it as a new section in the settings page, after the existing sections. Section heading: "Startup".

    **9. Wire first-launch dialog into page.tsx or layout.tsx:**
    On app mount, check `getAutoLaunchDialogShown()`. If false, show `<FirstLaunchDialog>`. This should run only once.
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && grep "tauri-plugin-autostart" crates/wallflower-app/Cargo.toml && grep "autostart" crates/wallflower-app/capabilities/default.json && grep -l "FirstLaunchDialog" src/components/settings/FirstLaunchDialog.tsx && grep -l "AutoLaunchSection" src/components/settings/AutoLaunchSection.tsx</automated>
  </verify>
  <acceptance_criteria>
    - crates/wallflower-app/Cargo.toml contains `tauri-plugin-autostart`
    - crates/wallflower-app/src/lib.rs contains `tauri_plugin_autostart::init` and `MacosLauncher::LaunchAgent`
    - crates/wallflower-app/capabilities/default.json contains `autostart:allow-enable` and `autostart:allow-disable` and `autostart:allow-is-enabled`
    - src/components/settings/FirstLaunchDialog.tsx contains `Start Wallflower on login?` and `Yes, auto-launch` and `Not now` and `enable` import
    - src/components/settings/AutoLaunchSection.tsx contains `isEnabled` and `enable` and `disable` and `Launch on login`
    - src/components/settings/SettingsPage.tsx contains `AutoLaunchSection` import
    - src/lib/tauri.ts contains `setAutoLaunchDialogShown` or `auto_launch_dialog_shown`
    - package.json contains `@tauri-apps/plugin-autostart`
  </acceptance_criteria>
  <done>Auto-launch on login works via Tauri autostart plugin. First launch dialog asks user preference. Settings page has toggle to enable/disable. All dialog copy matches UI spec.</done>
</task>

<task type="auto">
  <name>Task 2: GitHub Actions CI/CD pipeline for build, sign, notarize, and release</name>
  <files>
    .github/workflows/release.yml,
    crates/wallflower-app/tauri.conf.json
  </files>
  <read_first>
    crates/wallflower-app/tauri.conf.json,
    crates/wallflower-app/Cargo.toml,
    package.json
  </read_first>
  <action>
    **1. Create `.github/workflows/` directory** (does not exist yet).

    **2. Create `.github/workflows/release.yml`:**
    Per D-14: push to tag `v*` triggers build > sign > notarize > package .dmg > create GitHub Release.

    ```yaml
    name: Release

    on:
      push:
        tags: ['v*']

    permissions:
      contents: write

    jobs:
      build-macos:
        runs-on: macos-latest
        timeout-minutes: 60

        steps:
          - uses: actions/checkout@v4

          - name: Setup Node.js
            uses: actions/setup-node@v4
            with:
              node-version: 22
              cache: npm

          - name: Install Rust stable
            uses: dtolnay/rust-toolchain@stable
            with:
              targets: aarch64-apple-darwin,x86_64-apple-darwin

          - name: Cache Rust dependencies
            uses: Swatinem/rust-cache@v2
            with:
              workspaces: crates

          - name: Install frontend dependencies
            run: npm ci

          - name: Import Apple Developer Certificate
            env:
              APPLE_CERTIFICATE: ${{ secrets.APPLE_CERTIFICATE }}
              APPLE_CERTIFICATE_PASSWORD: ${{ secrets.APPLE_CERTIFICATE_PASSWORD }}
              KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
            run: |
              echo $APPLE_CERTIFICATE | base64 --decode > certificate.p12
              security create-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
              security default-keychain -s build.keychain
              security unlock-keychain -p "$KEYCHAIN_PASSWORD" build.keychain
              security set-keychain-settings -t 3600 -u build.keychain
              security import certificate.p12 -k build.keychain \
                -P "$APPLE_CERTIFICATE_PASSWORD" \
                -T /usr/bin/codesign
              security set-key-partition-list -S apple-tool:,apple:,codesign: \
                -s -k "$KEYCHAIN_PASSWORD" build.keychain
              rm certificate.p12

          - name: Build and release Tauri app
            uses: tauri-apps/tauri-action@v0
            env:
              GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
              APPLE_SIGNING_IDENTITY: ${{ secrets.APPLE_SIGNING_IDENTITY }}
              APPLE_ID: ${{ secrets.APPLE_ID }}
              APPLE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
              APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
            with:
              tagName: ${{ github.ref_name }}
              releaseName: 'Wallflower ${{ github.ref_name }}'
              releaseBody: 'See the assets below for the macOS installer (.dmg).'
              releaseDraft: true
              prerelease: false
              args: --target aarch64-apple-darwin
    ```

    **3. Update `crates/wallflower-app/tauri.conf.json` for signing:**
    Add/update the `bundle` section to include macOS-specific signing configuration. Find the existing `bundle` object and ensure it has:
    ```json
    {
      "bundle": {
        "active": true,
        "targets": ["dmg", "app"],
        "macOS": {
          "signingIdentity": null,
          "entitlements": null,
          "minimumSystemVersion": "13.0"
        }
      }
    }
    ```
    The `signingIdentity` is set to `null` because it's provided via the `APPLE_SIGNING_IDENTITY` environment variable at build time. `minimumSystemVersion` is "13.0" (macOS Ventura) to ensure SMAppService availability for the autostart plugin.

    Verify the `identifier` field is `com.wallflower.app` (it should already be set).
  </action>
  <verify>
    <automated>cd /Users/albair/src/wallflower && test -f .github/workflows/release.yml && grep "tauri-apps/tauri-action" .github/workflows/release.yml && grep "APPLE_SIGNING_IDENTITY" .github/workflows/release.yml && grep "minimumSystemVersion" crates/wallflower-app/tauri.conf.json</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/release.yml exists
    - .github/workflows/release.yml contains `on: push: tags: ['v*']`
    - .github/workflows/release.yml contains `tauri-apps/tauri-action@v0`
    - .github/workflows/release.yml contains `APPLE_CERTIFICATE` and `APPLE_SIGNING_IDENTITY` and `APPLE_ID` and `APPLE_PASSWORD` and `APPLE_TEAM_ID`
    - .github/workflows/release.yml contains `security create-keychain` and `security import`
    - .github/workflows/release.yml contains `releaseDraft: true`
    - .github/workflows/release.yml contains `aarch64-apple-darwin`
    - .github/workflows/release.yml contains `timeout-minutes: 60`
    - crates/wallflower-app/tauri.conf.json contains `minimumSystemVersion` with value `13.0` or higher
    - crates/wallflower-app/tauri.conf.json contains `com.wallflower.app`
  </acceptance_criteria>
  <done>GitHub Actions release workflow builds, signs, notarizes, and packages the app as a .dmg on push to a version tag. Tauri config is prepared for signing. User needs to add Apple Developer secrets to GitHub repository settings (documented in user_setup).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify auto-launch and CI/CD pipeline</name>
  <what-built>Auto-launch on login via Tauri autostart plugin with first-launch dialog and settings toggle. GitHub Actions CI/CD pipeline for signed macOS releases.</what-built>
  <how-to-verify>
    1. Run `cargo build -p wallflower-app` — should compile with autostart plugin
    2. Launch the app — first-launch dialog should appear asking about auto-launch
    3. Click "Yes, auto-launch" — dialog closes, setting saved
    4. Open Settings — "Launch on login" toggle should show enabled state
    5. Toggle off — should disable auto-launch
    6. Review `.github/workflows/release.yml` — should have the complete signing pipeline
    7. (Optional) If Apple Developer secrets are configured: push a `v0.x.0` tag to test the release workflow
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- `cargo build -p wallflower-app` compiles successfully with autostart plugin
- `.github/workflows/release.yml` exists and is valid YAML
- `grep -r "autostart" crates/wallflower-app/` returns plugin registration
- First-launch dialog text matches UI spec copywriting
</verification>

<success_criteria>
Auto-launch on login is configurable via Settings and first-launch dialog. GitHub Actions CI/CD pipeline is ready for signed macOS distribution (pending user adding Apple Developer secrets). Tauri config includes macOS bundle/signing fields.
</success_criteria>

<output>
After completion, create `.planning/phases/06-spatial-explorer-accessibility-distribution/06-04-SUMMARY.md`
</output>
